# Required
JWT_SECRET=change-me-to-a-random-string-at-least-32-chars
COOKIE_ENCRYPTION_KEY=change-me-to-a-64-character-hex-string-before-deploy-00000000000

# Optional: used only when the database has no admin user
INITIAL_ADMIN_EMAIL=admin@ncm.local
INITIAL_ADMIN_USERNAME=admin
INITIAL_ADMIN_PASSWORD=change-me-before-deploy
